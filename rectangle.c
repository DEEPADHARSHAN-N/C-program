#include <stdio.h>
int main(){
    int length = 10;
    int width = 5;
    int area = length * width;
    printf("The area of the rectangle with length %d and width %d is %d\n", length, width, area);
    return 0;
}